"""Fixtures for integration (route-level) tests.

Tests in this directory verify that multiple components work together correctly:
router, DI container, service layer, schema validation. The LLM dependency is
mocked — no credentials or real network calls needed.
"""
